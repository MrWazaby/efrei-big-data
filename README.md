# efrei-big-data
Big data project for Efrei

## requirements 
```
pip install pandas
pip install numpy
pip install matplotlib
pip install xlrd
```

You also need `tk` to display the graphs with pyplot.
